// Create socket connection
// window.socket = io.connect('https://' + window.location.host, {secure: true});

// Create crypto and session helpers
// window.CryptoHelper = new window.CryptoHelperLib();
// window.SessionHelper = new ConnectionHelperLib(socket, window.CryptoHelper);

// Custom heartbeat for the socket connection

